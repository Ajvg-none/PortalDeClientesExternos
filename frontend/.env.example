# Variables del frontend (Vite expone solo variables con prefijo VITE_).
# En Docker development el proxy /api apunta al servicio "api"; fuera de
# contenedores se puede sobrescribir, p. ej. http://localhost:3000
VITE_API_PROXY_TARGET=http://api:3000
